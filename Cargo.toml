[package]
name = "offsetter"
version = "1.0.0"
edition = "2024"
description = "Allows to easily create structs for gamehacking/other process injection work when only some of fields are known"
license = "MIT"

[dependencies]
paste = "1"